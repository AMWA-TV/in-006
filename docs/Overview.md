# AMWA IN-TEMPLATE: Template for AMWA Increments


_(c) AMWA 2026, CC Attribution-NoDerivatives 4.0 International (CC BY-ND 4.0)_

## Introduction

Write the text of this Increment using (GitHub Flavoured) Markdown documents where possible.

Put the documents in this directory and list any important entry points in the repository-root `README.md`.

Put diagrams (ideally PNG with encapsulated draw.io source) in the `images/` sub-directory.

Other types of content can be put in sub-directories of this directory, and linked in the Markdown text.

Update the repository name in the badges and repository links in the top-level `README.md` when creating a new Increment. The Zensical documentation workflow generates the rendered landing page from this file.

Set the value of `amwa_id` in `.render/_config.yml` to the AMWA-assigned ID.
